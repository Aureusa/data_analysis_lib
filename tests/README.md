# Tests

This directory contains tests for the project. Each test is designed to verify the functionality of specific components or features. To run the tests, you can use the following command:

```bash
pytest
```
